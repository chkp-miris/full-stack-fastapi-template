{'name': 'generate_code', 'arguments': {'code': 'import React from \'react\';\nimport { useQuery } from \'react-query\';\nimport { fetchAnalyticsData } from \'../services/analyticsService\';\nimport { ErrorBoundary } from \'../components/ErrorBoundary\';\nimport { Spinner } from \'../components/Spinner\';\nimport { AnalyticsChart } from \'../components/AnalyticsChart\';\n\n/**\n * AnalyticsDashboardPage Component\n * This component serves as the main dashboard for displaying analytics data.\n * It fetches data using React Query and displays it using the AnalyticsChart component.\n */\nconst AnalyticsDashboardPage: React.FC = () => {\n  // Fetch analytics data using React Query\n  const { data, isLoading, isError, error } = useQuery(\'analyticsData\', fetchAnalyticsData);\n\n  // Render loading state\n  if (isLoading) {\n    return (\n      <div className="flex justify-center items-center h-screen">\n        <Spinner />\n      </div>\n    );\n  }\n\n  // Render error state\n  if (isError) {\n    return (\n      <div className="flex justify-center items-center h-screen">\n        <p className="text-red-500">Error: {error instanceof Error ? error.message : \'An unknown error occurred.\'}</p>\n      </div>\n    );\n  }\n\n  // Render analytics data\n  return (\n    <ErrorBoundary>\n      <div className="p-4">\n        <h1 className="text-2xl font-bold mb-4">Analytics Dashboard</h1>\n        <AnalyticsChart data={data} />\n      </div>\n    </ErrorBoundary>\n  );\n};\n\nexport default AnalyticsDashboardPage;', 'language': 'typescript', 'imports': ["import React from 'react';", "import { useQuery } from 'react-query';", "import { fetchAnalyticsData } from '../services/analyticsService';", "import { ErrorBoundary } from '../components/ErrorBoundary';", "import { Spinner } from '../components/Spinner';", "import { AnalyticsChart } from '../components/AnalyticsChart';"], 'exports': ['AnalyticsDashboardPage'], 'dependencies': ['react', 'react-query', 'tailwindcss'], 'test_cases': [{'name': 'Renders loading state', 'input': 'Simulate isLoading = true', 'expected_output': 'Displays Spinner component'}, {'name': 'Renders error state', 'input': "Simulate isError = true with error message 'Network Error'", 'expected_output': "Displays error message 'Error: Network Error'"}, {'name': 'Renders analytics data', 'input': 'Simulate data = [{ id: 1, value: 100 }]', 'expected_output': 'Displays AnalyticsChart component with data'}], 'documentation': 'This file defines the AnalyticsDashboardPage component, which is the main page for displaying analytics data. It uses React Query to fetch data from the backend and displays it using the AnalyticsChart component. The component also includes error handling and a loading spinner for better user experience.', 'quality_score': 10}}