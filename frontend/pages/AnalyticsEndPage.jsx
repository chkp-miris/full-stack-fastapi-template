{'name': 'generate_code', 'arguments': {'code': 'import React from \'react\';\nimport { useQuery } from \'react-query\';\nimport { fetchAnalyticsData } from \'../api/analytics\';\nimport ErrorBoundary from \'../components/ErrorBoundary\';\nimport LoadingSpinner from \'../components/LoadingSpinner\';\n\n/**\n * AnalyticsEndPage Component\n * \n * This component displays analytics data fetched from the server.\n * It uses React Query for data fetching and includes error handling and loading states.\n * \n * @returns {JSX.Element} The rendered AnalyticsEndPage component.\n */\nconst AnalyticsEndPage = () => {\n    // Fetch analytics data using React Query\n    const { data, error, isLoading } = useQuery(\'analyticsData\', fetchAnalyticsData);\n\n    // Render loading state\n    if (isLoading) {\n        return (\n            <div className="flex justify-center items-center h-screen">\n                <LoadingSpinner />\n            </div>\n        );\n    }\n\n    // Render error state\n    if (error) {\n        return (\n            <div className="flex justify-center items-center h-screen">\n                <p className="text-red-500">An error occurred while fetching analytics data.</p>\n            </div>\n        );\n    }\n\n    // Render analytics data\n    return (\n        <div className="p-4">\n            <h1 className="text-2xl font-bold mb-4">Analytics Data</h1>\n            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">\n                {data.map((item) => (\n                    <div key={item.id} className="border p-4 rounded shadow">\n                        <h2 className="text-xl font-semibold">{item.title}</h2>\n                        <p>{item.description}</p>\n                    </div>\n                ))}\n            </div>\n        </div>\n    );\n};\n\nexport default function AnalyticsEndPageWithBoundary() {\n    return (\n        <ErrorBoundary>\n            <AnalyticsEndPage />\n        </ErrorBoundary>\n    );\n}', 'language': 'javascript', 'imports': ["import React from 'react';", "import { useQuery } from 'react-query';", "import { fetchAnalyticsData } from '../api/analytics';", "import ErrorBoundary from '../components/ErrorBoundary';", "import LoadingSpinner from '../components/LoadingSpinner';"], 'exports': ['AnalyticsEndPageWithBoundary'], 'dependencies': ['react', 'react-query', 'tailwindcss'], 'test_cases': [{'name': 'Render loading state', 'input': 'Simulate isLoading=true', 'expected_output': 'Displays LoadingSpinner component'}, {'name': 'Render error state', 'input': 'Simulate error=true', 'expected_output': 'Displays error message'}, {'name': 'Render analytics data', 'input': 'Provide mock analytics data', 'expected_output': 'Displays data in grid format'}], 'documentation': 'This file defines the AnalyticsEndPage component, which fetches and displays analytics data using React Query. It includes error handling, loading states, and integrates with Tailwind CSS for styling. The component is wrapped in an ErrorBoundary to catch and display errors gracefully.', 'quality_score': 10}}